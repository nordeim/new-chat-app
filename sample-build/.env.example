DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db
# Optional for local UI testing. Set NVIDIA_API_KEY in the server environment
# to enable real inference. Never expose it through NEXT_PUBLIC_* variables.
